# NocoDB — datos para el puente. Copia este archivo como .env.nocodb (sin .example) y rellena.
# El .env.nocodb NO se sube a git (esta en .gitignore). Aqui NO va ningun secreto real.
NOCODB_URL=https://tu-nocodb.tudominio.com
NOCODB_TOKEN=pega_aqui_tu_token_de_nocodb
